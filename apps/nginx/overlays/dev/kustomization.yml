apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  namespace: dev-nginx

resources:
  - ../../base

namePrefix: dev-

configMapGenerator:
  - name: hello-world-cm
    literals:
      - ENVIRONMENT=develop
